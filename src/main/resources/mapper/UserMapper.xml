<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finance_analysis.mapper.UserMapper">

    <resultMap id="UserMap" type="com.finance_analysis.model.User">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password_hash" property="passwordHash" />
        <result column="created_at" property="createdAt" />
    </resultMap>

    <insert id="insert" parameterType="com.finance_analysis.model.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password_hash, created_at)
        VALUES (#{username}, #{passwordHash}, #{createdAt})
    </insert>

    <select id="findByUsername" parameterType="string" resultMap="UserMap">
        SELECT id, username, password_hash, created_at
        FROM users
        WHERE username = #{username}
        LIMIT 1
    </select>

    <select id="findById" parameterType="long" resultMap="UserMap">
        SELECT id, username, password_hash, created_at
        FROM users
        WHERE id = #{id}
        LIMIT 1
    </select>

</mapper>
